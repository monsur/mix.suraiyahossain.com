# Sentry Configuration
# Get your DSN from: https://sentry.io/settings/[your-org]/projects/[your-project]/keys/
VITE_SENTRY_DSN=https://your-public-key@o0000000.ingest.sentry.io/0000000

# Sentry Source Map Upload (Optional - only needed for production builds)
# Get auth token from: https://sentry.io/settings/account/api/auth-tokens/
# Required scopes: project:releases, org:read
SENTRY_AUTH_TOKEN=your-auth-token-here
SENTRY_ORG=your-organization-slug
SENTRY_PROJECT=your-project-slug

# Optional: Custom release name (defaults to timestamp if not set)
# SENTRY_RELEASE=v1.0.0
